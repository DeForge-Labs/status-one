# =============================================================================
# Status One - Docker Compose Environment Configuration
# =============================================================================
# Copy this file to .env and customize the values below.
#   cp .env.example .env
# =============================================================================

# --- Ports -------------------------------------------------------------------
# Host ports for backend and frontend. Change if defaults conflict.
BACKEND_PORT=3000
FRONTEND_PORT=3001

# --- Backend -----------------------------------------------------------------
# IMPORTANT: Change this to a strong random string in production!
JWT_SECRET=change-me-to-a-random-secret

# --- SMTP (Email Notifications & Password Reset) ----------------------------
# Leave blank to disable email features.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Status One <noreply@example.com>"

# --- Frontend ----------------------------------------------------------------
# URL the browser uses to reach the backend API.
# If you're running locally: http://localhost:3000/api
# If behind a reverse proxy: https://yourdomain.com/api
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Domains which are not going to be used as custom status page domains
MAIN_DOMAINS=