NEXT_PUBLIC_API_URL=http://localhost:3000/api
# Public URL of this frontend (used for OpenGraph metadata and app_url setting).
# If omitted, derived automatically from NEXT_PUBLIC_API_URL.
NEXT_PUBLIC_APP_URL=http://localhost:3001
# Domains which are not going to be used as custom status page domains
MAIN_DOMAINS=